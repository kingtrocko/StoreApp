%h1 Listing carts

%table
  %thead
    %tr
      %th
      %th
      %th
      
  %tbody
    - @carts.each do |cart|
      %tr
        %td= link_to 'Show', [:store, cart]
        %td= link_to 'Edit', edit_store_cart_path(cart)
        %td= link_to 'Destroy', [:store, cart], :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Cart', new_store_cart_path
