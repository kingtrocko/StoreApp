= content_for :header do
  %h1 Editing unit: 

= content_for :buttons do
  <a href="#" onclick="$('#frm_units').submit();">Update</a>
  %span|
  = link_to 'Back', admin_units_path

= render 'form'

/= link_to 'Show', [:admin, @unit]
/\|
