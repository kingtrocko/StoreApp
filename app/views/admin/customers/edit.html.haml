= content_for :header do
  %h1 Editing customer: 

= content_for :buttons do
  <a href="#" onclick="$('#frm_customers').submit();">Update</a>
  %span|
  = link_to 'Back', admin_customers_path

= render 'form'

/= link_to 'Show', [:admin, @customer]
/\|
