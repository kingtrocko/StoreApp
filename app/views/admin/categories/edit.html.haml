= content_for :header do
  %h1 Editing category: 

= content_for :buttons do
  <a href="#" onclick="$('#frm_categories').submit();">Update</a>
  %span|
  = link_to 'Back', admin_categories_path

= render 'form'
/= link_to 'Show', [:admin, @category]
/\|

