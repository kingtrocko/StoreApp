= content_for :header do
  %h1 Editing product:

= content_for :buttons do
  <a href="#" onclick="$('#frm_products').submit();">Update</a>
  %span|
  = link_to 'Back', admin_products_path

= render 'form'

/= link_to 'Show', [:admin, @product]
/\|

